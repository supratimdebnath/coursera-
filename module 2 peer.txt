#include <stdio.h>

int fibonacci(int ,int,int);
int main()
{
    int a=0,b=1,size;
    printf("enter size u want");
    scanf("%d",&size);
    printf("\n%d%d",a,b);
    fibonacci(a,b,size-2);
    return 0;
}

int fibonacci(int x,int y,int z)
{
    printf("%d",x+y);
    if(z>0)
        fibonacci(y,x+y,z-1);
    return 0;
}
